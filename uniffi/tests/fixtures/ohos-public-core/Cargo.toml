[package]
name = "uniffi-ohos-public-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
uniffi = { path = "../../..", default-features = false, features = ["macro-scaffolding"] }

[workspace]
