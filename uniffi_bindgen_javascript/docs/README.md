# JavaScript Target Docs

This directory now keeps only crate-local documentation that must evolve
alongside the source tree.

The canonical JavaScript target contract lives in the manual:

- `docs/manual/src/javascript/contract.md`

Historical prompts, TODOs, reviews, and migration notes live outside the
repository:

- under the local external worklog directory for this fork

The split keeps the repository focused on versioned product documentation
while moving one-off workflow notes out of the fork.
